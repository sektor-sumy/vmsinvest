{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProductCategory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Created_date</th>
                <td>{{ entity.createddate|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>
        <a href="{{ path('category') }}" class="btn btn-link">
            Back to the list
        </a>
        <a href="{{ path('category_edit', { 'id': entity.id }) }}" class="btn btn-link">
            Edit
        </a>
   {{ form(delete_form) }}
{% endblock %}
